default: &default
    pool: 50

test:
    <<: *default
    user: root
    password: Admin.2@cd
    host: localhost
    port: 3306
    database: come_on_first

development:
    <<: *default
    user: $BOR_MYSQL_USER
    password: $BOR_MYSQL_PASSWORD
    host: $BOR_MYSQL_HOST
    port: $BOR_MYSQL_PORT
    database: $BOR_MYSQL_DATABASE